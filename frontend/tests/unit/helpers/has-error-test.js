
import { hasError } from 'frontend/helpers/has-error';
import { module, test } from 'qunit';

module('Unit | Helper | has error');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hasError([42]);
  assert.ok(result);
});

