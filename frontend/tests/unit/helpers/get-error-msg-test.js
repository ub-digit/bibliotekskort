
import { getErrorMsg } from 'frontend/helpers/get-error-msg';
import { module, test } from 'qunit';

module('Unit | Helper | get error msg');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getErrorMsg([42]);
  assert.ok(result);
});

